'use client'

import { FC } from 'react'

const Profile: FC = () => {
	// const { user } = useAuth()
	// const { data, isLoading } = useProfile()

	// return !isLoading ? (
	// 	<div>
	// 		<div>{data?.id}</div>
	// 		<div>{data?.email}</div>
	// 		<div>{data?.name}</div>
	// 		{/* <div>{user ? 'isAuth' : 'noAuth'}</div> */}
	// 	</div>
	// ) : (
	// 	<div>loading</div>
	// )

	return <div>Profile</div>
}

export default Profile
